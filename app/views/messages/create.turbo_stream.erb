    <%= turbo_stream.replace('new_message', partial: "messages/new",
                            locals: { message: Message.new(owner_id: @message.owner_id, 
                                            owner_type: @message.owner_type) }) %>

