<div class="form-group">
    <%= f.label field.name, field.name.humanize.titleize %>
    <% options = field.meta_data.present? ? field.meta_data.split(",") : [] %>
    <%= f.select field.name, options, {}, class: "form-control", required: field.required %>
</div>