json.extract! expression_of_interest, :id, :entity_id, :user_id, :eoi_entity_id, :InvestmentOpportunity_id, :amount_cents, :approved, :verified, :allocation_percentage, :allocation_amount_cents, :created_at, :updated_at
json.url expression_of_interest_url(expression_of_interest, format: :json)
