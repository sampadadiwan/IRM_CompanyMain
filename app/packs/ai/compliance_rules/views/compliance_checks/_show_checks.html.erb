<% if params[:compliance_checks].present? %>

    <% 
        model_class = controller_name.classify.constantize
        model = model_class.find(params[:id])
    %>

    <%= turbo_frame_tag "compliance_checks_frame", target: "_top", loading: :lazy, src: compliance_checks_path(owner_id: model.id, owner_type: model_class) do %>
        <h3 class="text-center text-primary">Loading ....</h3>
    <% end %>

<% end %>
