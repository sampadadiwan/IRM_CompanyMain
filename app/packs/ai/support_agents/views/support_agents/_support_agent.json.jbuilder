json.extract! support_agent, :id, :name, :description, :entity_id, :form_type_id, :agent_type, :json_fields, :document_folder_id, :created_at, :updated_at
json.url support_agent_url(support_agent, format: :json)
