<%= render "/layouts/alerts" %>

<% if params[:composite].present? %>
  <%= render "/reports/composite", report: @report %>
<% elsif params[:no_card].present? %>
  <%= render "/reports/index", reports: @reports %>
<% else %>
  <%= render "/reports/cards", reports: @reports %>
<% end %>