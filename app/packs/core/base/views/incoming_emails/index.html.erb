
<%= turbo_frame_tag "incoming_emails_list", target: "_top", loading: :lazy do %>    
    <%= render "/incoming_emails/index", incoming_emails: @incoming_emails if @incoming_emails %>
<% end %>
            