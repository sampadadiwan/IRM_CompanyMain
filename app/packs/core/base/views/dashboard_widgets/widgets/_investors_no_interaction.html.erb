<% 
  days = args[:days] || 30
  categories = args[:categories] 
%>
<%= render(
      WidgetCardComponent.new(
        widget: widget,
        args: args,
        widget_size: widget.widget_size
      )
    ) do %>
  <!-- This block becomes the `content` inside the component -->
  <%= turbo_frame_tag "investors_frame", target: "_top", src: "/investors?categories=#{categories}&not_interacted=#{days}&title=Investors: No Interaction" do %>            
  <%end%>
<% end %>
