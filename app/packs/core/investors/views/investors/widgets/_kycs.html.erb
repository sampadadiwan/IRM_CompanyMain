<% 
  widget_size ||= "col-md-6" 
  name ||= nil 
  args ||= {}
  uri_params ||= {}
  uri = URI("/investor_kycs?investor_id=#{@investor.id}")
  #uri.query = URI.encode_www_form(uri_params) if uri_params.present?
%>

<%= render(
      WidgetCardComponent.new(
        widget: widget,
        args: args,
        widget_size: widget.widget_size
      )
    ) do 
%>
    <%= turbo_frame_tag "investor_kycs_frame", target: "_top", src: "#{uri.to_s}" do %>            
    <%end%>
        
<% end %>