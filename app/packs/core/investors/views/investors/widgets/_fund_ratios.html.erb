<% uri = URI(fund_ratios_path(show_funds: true, q: ransack_query_params_multiple([[:owner_id, :eq, @investor.id], [:owner_type, :eq, "Investor"]]))) %>
<%= render(
      WidgetCardComponent.new(
        widget: widget,
        args: args,
        widget_size: widget.widget_size
      )
    ) do 
%>
    
    <%= turbo_frame_tag "fund_ratios_frame", target: "_top", src: "#{uri.to_s}" do %>            
    <%end%>
    <!-- This block becomes the `content` inside the component -->
    <%#= fund_ratios_line_chart(nil, owner_id: @investor.id, owner_type: "Investor", **widget.metadata_args) %>
  
<% end %>