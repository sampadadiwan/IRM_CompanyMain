<%= render(
      WidgetCardComponent.new(
        widget: widget,
        args: args,
        widget_size: widget.widget_size
      )
    ) do %>
  
    <!-- This block becomes the `content` inside the component -->
    <%= fund_ratios_line_chart(nil, owner_id: @investor.id, owner_type: "Investor", **widget.metadata_args) %>
  
<% end %>