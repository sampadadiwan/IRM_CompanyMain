

<%= render "/investor_notices/carousel" %>

<% allowed_dashboards = ["cxo_dashboard", "vc_dashboard", "fm_dashboard", "holding_dashboard", "investor_dashboard"] %>

<% if current_user.entity %>

    <% if params[:dashboard].present? &&  allowed_dashboards.include?(params[:dashboard]) %>
        <%= render params[:dashboard] %>
    <% else %>   
        <% if current_user.curr_role && current_user.curr_role.to_sym == :investor %>
            <%= render "vc_dashboard" %>
        <% elsif current_user.entity_type == "Company" %>
            <%= render "cxo_dashboard" %>        
        <% elsif current_user.entity.is_fund? %>
            <%= render "fm_dashboard2" %>
        <% elsif current_user.entity_type == "Family Office" || current_user.entity_type == "Investment Advisor" %>
            <%= render "investment_advisor_dashboard" %>
        <% end %>
    <% end %>
<% end %>