<p>Dear <%= @investor_access.user ? @investor_access.user.full_name : @investor_access.email %>!</p>

<p>

    This is a reminder to kindly add / update your KYC details by clicking on the button below.

    <p class="btn btn-outline-primary"><%= link_to  "Login to Access", new_investor_kyc_url("investor_kyc[investor_id]": @investor_access.investor_id, "investor_kyc[entity_id]": @investor_access.entity_id, "investor_kyc[user_id]": @investor_access.user_id, subdomain: @investor_access.entity.sub_domain) %></p>

</p>
