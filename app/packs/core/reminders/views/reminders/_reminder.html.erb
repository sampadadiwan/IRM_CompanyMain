<tr id="<%= dom_id reminder %>">
  <td>
    <%= reminder.note %>
  </td>
  <td>
    <%= reminder.email %>
  </td>
  <td>
    <%= l reminder.due_date %>
  </td>
  <td>
    <%= display_boolean(reminder.sent) %>
  </td>
  <td>
    <%= link_to "Show" , reminder , {class: "btn btn-primary" }%>
    <%= link_to "Edit" , edit_reminder_path(reminder) , {data: { turbo_frame: 'new_reminder' }, class: "btn btn-success" } if policy(reminder).update? %>
  </td>
</tr>