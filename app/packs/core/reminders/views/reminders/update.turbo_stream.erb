
    <%= turbo_stream.replace("#{dom_id @reminder}", 
        partial: "reminders/reminder", locals: { reminder: @reminder }) %>

    <%= turbo_stream.replace('toasts', partial: "layouts/success_toast", 
        locals: { message: "Added Reminder: #{@reminder.note}" }) %>
    

    <%= turbo_stream.replace('new_reminder', partial: "reminders/blank" ) %>
