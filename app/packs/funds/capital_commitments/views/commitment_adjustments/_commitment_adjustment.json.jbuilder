json.extract! commitment_adjustment, :id, :entity_id, :fund_id, :capital_commitment_id, :pre_adjustment, :amount, :post_adjustment, :reason, :created_at, :updated_at
json.url commitment_adjustment_url(commitment_adjustment, format: :json)
