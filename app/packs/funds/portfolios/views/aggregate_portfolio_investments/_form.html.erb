<%= form_with(model: aggregate_portfolio_investment) do |form| %>
  <% if aggregate_portfolio_investment.errors.any? %>
    <div class="alert alert-danger">
      <h2><%= pluralize(aggregate_portfolio_investment.errors.count, "error") %> prohibited this aggregate_portfolio_investment from being saved:</h2>

      <ul>
        <% aggregate_portfolio_investment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <%= form.text_field :entity_id, class: "form-control", hidden: true  %>
    <%= form.text_field :fund_id, class: "form-control", hidden: true  %>

  <div class="form-group">
    <%= form.label :portfolio_company_id %>
    <%= form.text_field :portfolio_company_id, class: "form-control"  %>
  </div>

  <div class="form-group">
    <%= form.label :quantity %>
    <%= form.text_field :quantity, class: "form-control"  %>
  </div>

  <div class="form-group">
    <%= form.label :fmv %>
    <%= form.text_field :fmv, class: "form-control"  %>
  </div>

  <div class="form-group">
    <%= form.label :avg_cost %>
    <%= form.text_field :avg_cost, class: "form-control"  %>
  </div>

  <div class="form-group">
    <%= form.submit t_common(:save), {class: "btn btn-outline-primary"}%>
  </div>
<% end %>
