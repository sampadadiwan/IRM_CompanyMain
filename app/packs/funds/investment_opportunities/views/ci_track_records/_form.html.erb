<%= render FormCard.new(title: "Track Record") do %>

  <%= form_with(model: ci_track_record, validate: true) do |form| %>
    <% if ci_track_record.errors.any? %>
      <div class="alert alert-danger">
        <h2><%= pluralize(ci_track_record.errors.count, "error") %> prohibited this ci_track_record from being saved:</h2>

        <ul>
          <% ci_track_record.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <%= form.text_field :owner_id, class: "form-control", hidden: true  %>
    <%= form.text_field :owner_type, class: "form-control", hidden: true  %>
    <%= form.text_field :entity_id, class: "form-control", hidden: true  %>

    <div class="form-group">
      <%= form.label :name %>
      <%= form.text_field :name, class: "form-control"  %>
    </div>


    <div class="form-group">
      <%= form.label :prefix %>
      <%= form.text_field :prefix, class: "form-control"  %>
    </div>

    <div class="form-group">
      <%= form.label :value %>
      <%= form.text_field :value, class: "form-control"  %>
    </div>


    <div class="form-group">
      <%= form.label :suffix %>
      <%= form.text_field :suffix, class: "form-control"  %>
    </div>

    <div class="form-group">
      <%= form.label :details %>
      <%= form.text_field :details, class: "form-control"  %>
    </div>

    <div class="form-group">
      <%= form.submit t_common(:save), {class: "btn btn-outline-primary"}%>
    </div>
  <% end %>

<% end %>
