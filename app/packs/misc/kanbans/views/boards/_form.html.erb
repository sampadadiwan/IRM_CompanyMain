<%= render FormCard.new(title: "New Kanban Board") do %>

  <%= form_with(model: kanban_board, validate: true) do |form| %>
    <% if kanban_board.errors.any? %>
      <div class="alert alert-danger">
        <h2><%= pluralize(kanban_board.errors.count, "error") %> prohibited this kanban_board from being saved:</h2>

        <ul>
          <% kanban_board.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <%= form.text_field :entity_id, class: "form-control", hidden: true  %>


      <div class="form-group">
          <%= form.label :name, "Name" %>
          <%= form.text_field :name, class: "form-control"  %>
      </div>

    <div class="form-group">
      <%= form.submit t_common(:save), {class: "btn btn-outline-primary"}%>
    </div>
  <% end %>

<% end %>
