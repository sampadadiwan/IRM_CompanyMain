<tr id="<%= dom_id offer %>">
  <td>
    <%= offer.user.full_name %>
  </td>
  <td>
    <%= offer.investor.investor_name %>
  </td>
  <% if show_secondary_sale %>
  <td>
    <%= offer.secondary_sale.name %>
  </td>
  <% end %>
  <td>
    <%= offer.quantity %>
  </td>
  <td>
    <%= offer.percentage %>%
    <% if offer.percentage && offer.percentage > offer.secondary_sale.percent_allowed %>
      <span class="badge badge-danger">Over Allowed Percentge</span>
    <% end %>
  </td>
  <td>
    <%= custom_format_number offer.allocation_quantity, params %>
  </td>
  <td>
    <%= offer.allocation_percentage %>%
  </td>
  <td>
    <%= custom_format_number offer.final_price, params %>
  </td>
  <td>
    <%= money_to_currency offer.allocation_amount, params %>
  </td>
  <td>
    <%= offer.notes %>
  </td>
  <td class="approved">
    <%= display_boolean(offer.approved) %>
  </td>
  <td class="verified">
    <%= display_boolean(offer.verified) %>
  </td>
  <td>
    <%= link_to "Show" , offer , {class: "btn btn-primary" } if policy(offer).show? %>

    <% if policy(offer).update? %>
      <%= link_to "Complete Offer Details" , edit_offer_path(offer) , {class: "btn btn-warning" } if offer.approved %>    
      <%= button_to "Delete", offer, method: :delete, class: "btn btn-danger", form_class: "deleteButton", data: {action: "click->confirm#popup"} if policy(offer).destroy? %>    
    <% end %>
  </td>
</tr>