<tr id="offer_<%= offer.id %>">

  <%= render RansackTableRow.new(model: offer, columns:) %>

  <td>
    <%= link_to  t_common(:show) , offer_path(offer) , {class: "btn btn-outline-primary ti ti-eye" }%>
    <%= link_to  "Complete Offer Details" , edit_offer_path(offer) , {class: "btn btn-outline-warning ti ti-check" }  if kpi_report.approved && policy(kpi_report).edit?%>
  <td>
</tr>
