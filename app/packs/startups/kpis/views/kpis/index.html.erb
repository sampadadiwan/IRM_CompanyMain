
<%= render "/layouts/filter", associations: [:kpi_report, :entity, :investor_kpi_mappings, :portfolio_company], hidden_fields: {grid_view: params[:grid_view]} %>

<%= render ReportingComponent.new do %>

    <% if params[:grid_view].present? %>    
            <%= render "/kpis/grid_view", kpis: @kpis %>    
    <% else %>
        <%= render "/kpis/index", kpis: @kpis %>
    <% end %>

<% end %>

